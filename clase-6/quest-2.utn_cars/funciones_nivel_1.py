from utn_fra.datasets import (
        lista_autos_marcas,
        lista_autos_modelos,
        lista_autos_cantidades,
        lista_autos_precios
    )


# Recorrer la lista imprimiendo por consola el modelo de autos que hay en cada garage

def imprimir_modelos(lista_modelos)